# Application dependencies

require:
    - play
    - resolve -> resolve
    
repositories:
    - resolve:
        type:       local
        artifact:   ${application.path}/lib/[module].jar
        contains:
            - resolve
